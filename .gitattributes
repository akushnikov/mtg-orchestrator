# Files executed inside Linux containers / by bash on the VPS MUST keep LF
# line endings. core.autocrlf can otherwise smuggle CRLF into blobs on Windows,
# which breaks `sh entrypoint.sh` (\r in args) and bash scripts.
*.sh   text eol=lf
*.py   text eol=lf
*.j2   text eol=lf
*.toml text eol=lf
